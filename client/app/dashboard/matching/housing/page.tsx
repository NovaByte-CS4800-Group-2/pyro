"use client"

import MatchingForm from "@/app/ui/matchingform";

export default function Housing() {

	return (
		<div className="w-full h-full flex flex-col items-center">
			<MatchingForm type={0}></MatchingForm>
		</div>
	);
}